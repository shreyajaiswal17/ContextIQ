# Leave empty in local development to automatically use Vite proxy (/api -> localhost:5000)
# In production (e.g. Vercel), set this to your deployed backend URL.
# VITE_API_URL=https://contextiq-tofd.onrender.com
VITE_API_URL=
