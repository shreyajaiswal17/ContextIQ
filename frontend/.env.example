# Leave empty in local development to automatically use Vite proxy (/api -> localhost:5000)
# In production (e.g. Vercel), set this to your deployed backend URL (e.g. https://contextiq-api.onrender.com)
VITE_API_URL=
